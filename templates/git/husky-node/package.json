{
  "name": "{{solution-name-lower}}-tooling",
  "private": true,
  "type": "module",
  "description": "Root tooling manifest: git hooks (Husky), commit linting (commitlint), staged formatting (lint-staged). Not an application package.",
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.6.1",
    "@commitlint/config-conventional": "^19.6.0",
    "husky": "^9.1.7",
    "lint-staged": "^15.3.0"
  }
}
